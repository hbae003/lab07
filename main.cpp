#include "SortAlgorithm.h"
#include "Container.h"

#include <iostream>
#include <vector>
#include <list>

using namespace std;

int main()
{
    //  Declare a vector
    vector <int> v;
    v.push_back(3);
    v.push_back(5);
    v.push_back(2);
    v.push_back(1);
    v.push_back(4);

    //  Declare a list
    list <int> l;
    l.push_back(3);
    l.push_back(5);
    l.push_back(2);
    l.push_back(1);
    l.push_back(4);

    return 0;
}
